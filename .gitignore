# Build output
lightburn_tray_mac
lightburn_tray.exe

# macOS app bundle (built by build.sh)
LightBurnMonitor.app/

# Generated icon (rebuilt by make_icon.py / build.sh)
AppIcon.icns
AppIcon.iconset/

# Nim cache
nimcache/

# Local settings — contains SMTP credentials, never commit
lightburn_tray.json
